{
  "identity": {
    "name": "@epdoc/strava-schema",
    "purpose": "TypeScript types and type guards for the Strava API",
    "type": "utility",
    "complexity": "moderate"
  },
  "quickStart": {
    "pattern": "Import namespaces, use type guards for API responses",
    "code": "import * as Schema from '@epdoc/strava-schema';\nif (Schema.Activity.isDetailed(data)) {\n  const act: Schema.Activity.Detailed = data;\n  console.log(act.name, act.distance);\n}"
  },
  "exports": [
    {
      "name": "Schema.Activity",
      "role": "namespace",
      "description": "Activity types (Detailed, Summary, Lap, etc.) and guards"
    },
    {
      "name": "Schema.Athlete",
      "role": "namespace",
      "description": "Athlete types (Detailed, Summary, Club) and guards"
    },
    {
      "name": "Schema.Segment",
      "role": "namespace",
      "description": "Segment types (Detailed, Summary, Effort) and guards"
    },
    {
      "name": "Schema.Stream",
      "role": "namespace",
      "description": "Stream types (Data, LatLng, Set) and guards"
    },
    {
      "name": "Schema.Consts",
      "role": "namespace",
      "description": "Constants: ActivityName, StreamKeys, type ID guards"
    },
    {
      "name": "Schema.Types",
      "role": "namespace",
      "description": "Core types: StravaId, PolylineMap, MetaAthlete, etc."
    }
  ],
  "patterns": [
    {
      "name": "Validate API Response",
      "when": "Processing Strava API response data",
      "code": "if (Schema.Activity.isSummaryArray(data)) {\n  return data;\n}\nthrow new Error('Invalid data');"
    }
  ],
  "relationships": {
    "dependsOn": ["@epdoc/type"],
    "usedBy": ["@epdoc/strava-api"]
  }
}
